"""
Backend API Tests Package

Parallel API tests for Dumont Cloud backend.
These tests use the API directly (not CLI) for faster execution.

Run with pytest-xdist for parallel execution:
    pytest tests/backend/api -n 10 -v
"""
